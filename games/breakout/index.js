const{createInput}=window.ArcadeInput;const{highScore,saveHighScore}=window.ArcadeStorage;
window.ArcadeGames=window.ArcadeGames||{};window.ArcadeGames['breakout']={init(host){host.innerHTML='<div class="game-root"><div class="game-hud"><span>Score <strong id="s">0</strong></span><span>Best <strong id="b"></strong></span></div><canvas width="640" height="440"></canvas><p class="hint">Move with ← →</p></div>';const c=host.querySelector('canvas'),g=c.getContext('2d'),input=createInput(),s=host.querySelector('#s'),b=host.querySelector('#b');let pad=280,ball={x:320,y:340,vx:3.5,vy:-3.5},bricks=Array.from({length:40},(_,i)=>({x:20+i%8*75,y:35+Math.floor(i/8)*28,on:true})),score=0,raf,live=true;b.textContent=highScore('breakout');function frame(){if(input.down('ArrowLeft'))pad=Math.max(0,pad-7);if(input.down('ArrowRight'))pad=Math.min(560,pad+7);ball.x+=ball.vx;ball.y+=ball.vy;if(ball.x<7||ball.x>633)ball.vx*=-1;if(ball.y<7)ball.vy*=-1;if(ball.y>405&&ball.x>pad&&ball.x<pad+80)ball.vy=-Math.abs(ball.vy);bricks.forEach(q=>{if(q.on&&ball.x>q.x&&ball.x<q.x+65&&ball.y>q.y&&ball.y<q.y+20){q.on=false;ball.vy*=-1;score+=10;s.textContent=score;b.textContent=saveHighScore('breakout',score)}});if(ball.y>450)live=false;g.fillStyle='#090d1b';g.fillRect(0,0,640,440);bricks.filter(q=>q.on).forEach((q,i)=>{g.fillStyle=`hsl(${i*13},85%,62%)`;g.fillRect(q.x,q.y,65,20)});g.fillStyle='#ffe66d';g.fillRect(pad,410,80,12);g.fillStyle='#48d7ff';g.beginPath();g.arc(ball.x,ball.y,7,0,7);g.fill();if(live)raf=requestAnimationFrame(frame);else{g.fillStyle='white';g.font='bold 25px system-ui';g.fillText('Ball lost',270,240)}}raf=requestAnimationFrame(frame);return{destroy(){cancelAnimationFrame(raf);input.dispose()}}}}

